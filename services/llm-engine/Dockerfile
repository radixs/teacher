# syntax=docker/dockerfile:1.6
# Placeholder Dockerfile for llama.cpp-based LLM engine.
# TODO: install llama.cpp with HIP support, download GGUF model, expose inference server.

FROM alpine:3.19 AS todo

CMD ["/bin/sh", "-c", "echo LLM engine image not yet implemented; exit 1"]
