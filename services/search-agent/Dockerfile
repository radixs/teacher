# syntax=docker/dockerfile:1.6
# Placeholder Dockerfile for DuckDuckGo search agent.
# TODO: install Python runtime, scraping dependencies, and command runner.

FROM alpine:3.19 AS todo

CMD ["/bin/sh", "-c", "echo Search agent image not yet implemented; exit 1"]
