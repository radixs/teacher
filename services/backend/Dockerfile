# syntax=docker/dockerfile:1.6
# Placeholder Dockerfile for Laravel backend service.
# TODO: install PHP 8.3, Composer, project dependencies, and runtime configuration.

FROM alpine:3.19 AS todo

CMD ["/bin/sh", "-c", "echo Backend image not yet implemented; exit 1"]
