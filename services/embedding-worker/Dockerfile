# syntax=docker/dockerfile:1.6
# Placeholder Dockerfile for embedding worker service.
# TODO: install Python runtime, sentence-transformers, and expose embedding API.

FROM alpine:3.19 AS todo

CMD ["/bin/sh", "-c", "echo Embedding worker image not yet implemented; exit 1"]
