# Rollout Plan

| Step | Description | Status | Notes |
| --- | --- | --- | --- |
| 1 | Establish documentation + governance scaffolding (`AGENTS.md`, README stub, `.env` templates) | completed | Added `AGENTS.md`, README scaffold, and `.env.dist`. |
| 2 | Define container orchestration skeleton (docker-compose, Makefile targets, base Dockerfiles) | completed | Added compose file, Makefile, placeholders for service Dockerfiles. |
| 3 | Scaffold backend Laravel 12 project with service boundaries + config stubs | completed | Added Laravel skeleton, service wiring, env/config stubs, Dockerfile + entrypoint. |
| 4 | Scaffold Vue 3 + Vuex SPA with chat shell + API adapters | completed | Added Vite Vue app, store, chat UI, Docker entrypoint + compose wiring. |
| 5 | Build Python RAG orchestrator service (FastAPI) with embedding + LLM client interfaces | completed | Added FastAPI app, in-memory session manager, HTTP clients & Docker image. |
| 6 | Package llama.cpp service with model download/init scripts for Mistral 7B Q4_K_M | completed | Added llama.cpp build image, config-driven entrypoint, compose volume wiring. |
| 7 | Package embedding worker using `bge-base-en-v1.5` and expose embedding endpoint | completed | Added FastAPI embed service, model caching, Docker image + compose volume. |
| 8 | Configure Elasticsearch 9.1.4 container, index templates, and bootstrap scripts | completed | Added index templates, bootstrap script, compose volume + Make target. |
| 9 | Implement DuckDuckGo search agent + web scraper for resource ingestion | completed | Added FastAPI search service, scraper, Docker image, compose wiring. |
| 10 | Implement calibration phase flows (profile capture, diagnostic Q&A, persistence) | completed | Added calibration planner, stateful session flow, ES persistence for answers. |
| 11 | Implement tuning program generator (dependency graph authoring + storage) | completed | Added tuning generator, ES persistence, session phase transition. |
| 12 | Implement learning phase (lesson delivery, resource linking, exercise prompts) | completed | Added learning coordinator, lesson messaging, progress tracking + tests. |
| 13 | Implement exercise grading pipeline + configurable rubrics | completed | Added LLM-backed grader, YAML rubrics, learning flow integration + tests. |
| 14 | Build lab primer generator + temp recipe handling | completed | Added lab templates, `/lab` command handling, optional auto-write to temp labs. |
| 15 | Implement automated test suites (`make test`: containers, API with stubs, RAG integration, UI e2e) | completed | `make test` orchestrates backend/phpunit, Python pytest suites, frontend build smoke. |
| 16 | Finalize documentation (README usage guide, update rollout + agents, polish) | completed | README quick-start added; docs & guides refreshed. |
| 17 | Add deep flow guide + single-file demo flow logging across owned services | completed | Added `detailed_flow_for_dummies.md`, shared `FLOW_LOG_PATH` wiring, and flow-aware logging in backend, rag-orchestrator, embedding-worker, search-agent, and llm-engine startup. |
| 18 | Activate LLM-driven calibration/tuning, search-backed roadmap generation, and hot-path writes for `user_profiles` + `learning_resources` | completed | `rag-orchestrator` now generates calibration questions and roadmaps through `llm-engine` with fallback logic, calls `search-agent` during tuning, stores profile/resource vectors in Elasticsearch, aligns index templates with runtime writes, and refreshes docs. |
| 19 | Add manual frontend-based verification guide with expected shared-log markers | completed | Added `manual_tests.md` covering startup, session start, calibration, roadmap generation, learning pass/retry, `/lab`, resume, Kibana inspection, and warning/fallback log lines. |
| 20 | Harden shared demo-log permissions for host + container writes | completed | `truncate-log` now recreates the log file with world-writable mode, startup targets prepare `infrastructure/demo-logs` with permissive directory mode, and service loggers normalize file permissions before appending. |
| 21 | Fix Elasticsearch bootstrap flow and surface stale-index mapping problems | completed | Fixed `bootstrap.sh` loop syntax, propagated `ELASTICSEARCH_URL` from the host wrapper, added cluster readiness waiting and idempotent index checks, removed obsolete `index.knn` template settings for Elasticsearch 9.1.4, and documented recovery when old indices exist without mappings. |
| 22 | Make startup logging reliable on fresh boots | completed | `make up`/`make restart` now rebuild image-backed services, `rag-orchestrator` no longer crashes if Elasticsearch is still starting or the sessions index is not bootstrapped yet, and manual startup expectations were updated to include waiting/bootstrap-pending states. |
| 23 | Handle Elasticsearch warm-up `503` responses during orchestrator startup | completed | `rag-orchestrator` now retries through temporary Elasticsearch `503` responses before declaring startup complete, and the manual test guide now notes that `startup.completed` can lag behind other startup lines by 10-20 seconds on fresh restarts. |
| 24 | Fix degraded manual-flow paths for search, embeddings, and backend request handling | completed | Search queries are now truncated to a safe budget and DuckDuckGo redirects are followed, embedding-worker no longer crashes while reporting the model id, Laravel waits longer for rag-orchestrator and returns a controlled `504` if it still times out, and LLM roadmap/grading calls use smaller response budgets with fallback-friendly timeouts. |
| 25 | Remove slow page-scraping from the learner-facing roadmap hot path | completed | `rag-orchestrator` still uses real DuckDuckGo search results for roadmap generation, but it now calls `search-agent` with `enrich=false` so synchronous frontend requests do not wait on scraper fetches; flow docs and manual log expectations were updated accordingly. |
| 26 | Switch `llm-engine` from CPU fallback to HIP GPU offload by default | completed | Set `LLM_GPU_LAYERS=32`, corrected the Dockerfile from stale `LLAMA_HIPBLAS` flags to the current `GGML_HIP` / `GGML_HIPBLAS` build path, upgraded containerized ROCm to `6.1.2`, and verified live startup logs showing the RX 6600 plus `offloaded 32/33 layers to GPU`. |
| 27 | Add a single `.env` acceleration switch for `llm-engine` | completed | Added `LLM_ACCELERATION_MODE=gpu|cpu`, made the entrypoint translate `cpu` into `gpu_layers=0` plus `HIP_VISIBLE_DEVICES=-1`, and updated docs so GPU/CPU mode changes only require `.env` + `make restart`. |
| 28 | Surface the shared flow events directly inside the frontend | completed | Added a backend SSE stream with a rolling in-memory-style buffer backed by cache, made backend/rag/search/embedding/llm loggers push events alongside the file log, and added the `Live Flow Console` panel at the bottom of the chat UI so demos no longer require tailing the host log file. |
| 29 | Polish GUI timestamps and bottom-stick scrolling for live demos | completed | Switched visible frontend timestamps to 24-hour formatting and made the chat history plus live flow console stay pinned when the user is already at the bottom and new entries appear. |
| 30 | Separate normal container startup from explicit image rebuilds in the Makefile | completed | `make up` and `make restart` now use existing images without forcing `--build`, while `make rebuild` and `make restart-build` provide explicit rebuild paths when image-baked files change. |
